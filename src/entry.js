//Require js files here that the client-side html files depend on.
//These files will all get imported into public/js/packed.js using webpack

//To see an example go to public/js/packed.js and you will see this code has been included there
var foo = "foo";
console.log(foo);
